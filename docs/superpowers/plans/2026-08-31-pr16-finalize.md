# Finalizar EPUB Reverse-Scroll Stall Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:executing-plans to implement this plan.

**Goal:** Prove or reject the ContinuousManager queue hypothesis on the current main and keep only an evidence-backed EPUB reverse-scroll fix.

**Architecture:** Keep diagnostics opt-in and isolate them from the normal runtime path. Replace fragile queue-task string matching with a small, testable coordinator that permits one running check plus one trailing request, then validate the behavior against the real Android WebView before deciding whether the coordinator remains.

**Tech Stack:** JavaScript WebView runtime, epub.js, Vitest, React Native Android example, ADB emulator `emulator-5554`.

---

### Task 1: Audit and baseline the current branch

**Files:**
- Inspect: `packages/ui-react-native/runtime/runtime.js`
- Inspect: `packages/ui-react-native/runtime/comicRuntime.test.ts`
- Inspect: `packages/ui-react-native/runtime/index.html`

- [ ] Confirm the branch is based on current `origin/main` and diff contains only EPUB changes.
- [ ] Identify the current queue patch, diagnostics lifecycle, selection lifecycle, and generated-runtime sync path.
- [ ] Prepare the Android EPUB fixture/procedure and capture baseline evidence before changing queue behavior.

### Task 2: Add failing coordinator and lifecycle tests

**Files:**
- Modify: `packages/ui-react-native/runtime/comicRuntime.test.ts`
- Create if useful: `packages/ui-react-native/runtime/epubScrollCoordinator.test.ts`

- [ ] Test one running check plus one trailing check for concurrent requests.
- [ ] Test synchronous and rejected checks clear coordinator state.
- [ ] Test selection transitions to false on clear/end and reset on new document/cleanup.
- [ ] Run focused tests and confirm the new tests fail before implementation.

### Task 3: Implement the minimal runtime change

**Files:**
- Modify: `packages/ui-react-native/runtime/runtime.js`
- Regenerate: `packages/ui-react-native/runtime/index.html`

- [ ] Keep all PR19 load/binary/generation behavior from `main`.
- [ ] Keep diagnostic messages opt-in and avoid timers/touch bookkeeping when disabled.
- [ ] Replace `String(task)`/`this.check()` detection with an explicit coordinator around the supported check entry point, if the epub.js runtime exposes one.
- [ ] Preserve a trailing check and clear state on sync throw/rejection.
- [ ] Complete selection lifecycle and cleanup.
- [ ] Run focused tests and syntax/build checks.

### Task 4: A/B validation on Android

**Files:**
- Modify: `docs/performance/pr-16-epub-reverse-scroll.md`

- [ ] Build/install the debug example and use only `emulator-5554`.
- [ ] Run instrumented baseline with queue behavior original and candidate with coordinator.
- [ ] Execute the required down/up, immediate reverse, alternating, chapter-boundary, and momentum scenarios.
- [ ] Record stall count, check request/start/end, queue evidence, and check duration distributions.
- [ ] If no queue/stall correlation exists, remove the coordinator and retain only useful instrumentation.

### Task 5: Final verification and publication

- [ ] Run focused tests, package build, and `node --check`.
- [ ] Confirm no PDF/native files changed.
- [ ] Commit, push a new PR because original PR16 is already merged, and report baseline/candidate evidence and limitations.
