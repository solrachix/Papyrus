# Papyrus Mobile (Expo)

Expo example that uses the native engine via `expo prebuild` and a dev client.

## Quick start

```bash
cd examples/mobile-expo
npm install
expo prebuild
expo run:android
```

For iOS builds, use `expo run:ios` or EAS Build.
